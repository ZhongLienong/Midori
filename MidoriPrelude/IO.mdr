namespace IO
{
	foreign "Print" Print : (Text) -> Unit;

	foreign "OverwriteToFile" OverwriteToFile : (Text, Text) -> Bool;

	foreign "AppendToFile" AppendToFile : (Text, Text) -> Bool;

	def PrintLine : (Text) -> Unit = fn(text) -> Print(text ++ "\n");
}