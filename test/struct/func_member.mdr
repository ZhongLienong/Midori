module FuncMember

import
{
	"../../MidoriPrelude/IO.mdr",
}

struct FunStruct
{
	num : Int,
	no_op : fn(Int) -> Int,
	inc : fn() -> Unit,
	plus : fn(Int) -> Int,
};

struct Fib
{
	n : Int,
	calc : fn(Int) -> Int
};

def s = new FunStruct
(
	0, 
	fn(x : Int) : Int => x,
	fn() : Unit => (),
	fn(x : Int) : Int => x + x
);