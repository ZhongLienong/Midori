module RecordUpdateTypeMismatch

// A value whose type does not match the field's is a compile error.

type Point =
{
	x : Int,
	y : Int
};

def p = Point(1, 2);
def bad = { p with x = "not an int" };
