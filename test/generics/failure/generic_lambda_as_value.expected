Compilation failed :(
Code Generator Error at test/generics/failure/generic_lambda_as_value.mdr:15
   |
15 | IO::PrintLine(apply(Id, 5) as Text);
   |                     ^^ Generic function 'Id' is monomorphised at each call site, so it has no single procedure to use as a value. Call it directly, or wrap it in a non-generic lambda that pins its type parameters.
   |
