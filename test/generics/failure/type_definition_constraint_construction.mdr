module TypeDefinitionConstraintConstruction

class Show<T> {
    show: fn(value: T) -> Text;
};

type Box<T> where Show<T> = {
    value: T
};

type Hidden = {
    value: Int
};

def bad = Box(Hidden(1));
