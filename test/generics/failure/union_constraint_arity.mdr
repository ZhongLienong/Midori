module UnionConstraintArity

// Test: Constraint type argument count mismatch
// Expected: SHOULD FAIL - Show expects 1 type argument

class Show<T> {
    show: fn(value: T) -> Text;
};

union Option<T> where Show<T, Int> = Some(T) | None;
