module MatchWildcardIdentifierOutsidePattern

import
{
	"../../../MidoriPrelude/IO.mdr",
}

def _ = 9;

def result = match (1, 2) with
	case (_, value) => _ + value
;

IO::PrintLine("wildcard_identifier_result=" ++ (result as Text));
