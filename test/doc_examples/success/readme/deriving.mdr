module ReadmeDeriving

import { "../MidoriPrelude/Equatable.mdr", "../MidoriPrelude/Hashable.mdr" }

type Point = {
    x: Int,
    y: Int
} deriving (Equatable, Hashable);

type OptionBox<T> = Empty | Full(T) deriving (Map, Bind, Unwrap);
