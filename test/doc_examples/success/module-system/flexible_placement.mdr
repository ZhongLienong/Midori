module Example

def LocalHelper = fn(x: Int) -> Int => x + 1;

import { <IO> }

public export { main }

use IO.{PrintLine}

def main = fn() -> Int => {
    PrintLine((LocalHelper(41)) as Text);
    0
};
