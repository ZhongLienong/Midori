module worker_spawn_syntax

import { "../../../MidoriPrelude/IO.mdr" }
use IO.{PrintLine}

defun WorkerTask(message: Text) : Int => {
    if message == "hello" then 42 else 0
};

def worker = spawn WorkerTask("hello");
def result = join worker;

PrintLine("Worker result: " ++ (result as Text));
