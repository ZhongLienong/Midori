module MakeChannelRequiresElementType

import { "../../../MidoriPrelude/Concurrency.mdr" }

// Concurrency::MakeChannel(capacity) names no element type, so it takes the one its
// context expects. With no annotation there is none, and the error shows where to
// write it.

def ch = Concurrency::MakeChannel(4);
