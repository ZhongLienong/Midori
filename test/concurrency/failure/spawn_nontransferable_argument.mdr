module spawn_nontransferable_argument

defun RunCallback(callback: fn(Int) -> Int) : Int => {
    callback(1)
};

def increment = fn(value: Int) => {
    value + 1
};

def worker = spawn RunCallback(increment);
