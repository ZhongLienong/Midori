module StressTest

// Test HashMap with many elements to trigger rehashing
import 
{
    "../../../MidoriPrelude/IO.mdr",
    "../../../MidoriPrelude/IO.mdr",
    "../../../MidoriPrelude/Collections/Map.mdr",
    "../../../MidoriPrelude/Prelude/Option.mdr"
}
use Option.{Option}

def numbers : Map::Map<Int, Int> = Map::MapNew();

// Insert 100 key-value pairs (key -> key * 2)
for i in 0..1..100 {
    Map::MapInsert(numbers, i, i * 2);
};

IO::PrintLine("Size after 100 inserts: " ++ (Map::MapCount(numbers) as Text));

// Verify all values are correct
def all_correct = {
    def count = 0;
    for i in 0..1..100 {
        match Map::MapGet(numbers, i) with
            case Option::Some(value) => {
                if value == i * 2 then {
                    count <- count + 1;
                } else {
                    ()
                }
            }
            case Option::None() => ()
        ;
    };
    count
};

IO::PrintLine("Correct values: " ++ (all_correct as Text));

// Check non-existent keys
match Map::MapGet(numbers, 200) with
    case Option::Some(_) => IO::PrintLine("Found 200: true")
    case Option::None() => IO::PrintLine("Found 200: false")
;

match Map::MapGet(numbers, -1) with
    case Option::Some(_) => IO::PrintLine("Found -1: true")
    case Option::None() => IO::PrintLine("Found -1: false")
;

IO::PrintLine("hashmap_stress: PASS");

