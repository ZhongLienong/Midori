module PrivateHelperInGeneric

import { "private_helper_in_generic_lib.mdr", "../../../MidoriPrelude/IO.mdr" }

def ints = [1, 2, 3];
def words = ["a", "b"];

IO::PrintLine(PrivateHelperInGenericLib::Scale(ints, 10) as Text);
IO::PrintLine(PrivateHelperInGenericLib::Scale(words, 20) as Text);
IO::PrintLine(PrivateHelperInGenericLib::Describe(ints, 30));
