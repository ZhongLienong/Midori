module SystemImportMultiple

// Test: multiple system imports

import { <IO>, <Math> }

use IO.{PrintLine}
use Math.{SquareRoot}

def main = fn() -> Unit => {
    PrintLine("sqrt(16) = " ++ (SquareRoot(16.0) as Text));
};

main();
