// Test: File without module declaration should still work
// No module declaration - file name becomes module name

defun simple_add(a: Int, b: Int) : Int => a + b;

defun main() : Int => simple_add(10, 20);
