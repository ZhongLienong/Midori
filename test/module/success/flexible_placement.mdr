module FlexiblePlacement

defun greet(name: Text): Text => {
    "Hello, " ++ name
};

import
{
    <IO>
}

defun calculate(x: Float): Float => {
    x * 2.0
};

use IO.{PrintLine}

defun main(): Unit => {
    PrintLine(greet("World"));
    PrintLine(calculate(21.0) as Text);
};

main();
