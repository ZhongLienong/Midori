module ReturnRemoved

// `return` was removed: a function's value is its body's value, and a block's
// value is its last expression. The error names that replacement rather than
// failing with a bare "Undefined name.", since `return` now lexes as an ordinary
// identifier.

def Square = fn(n : Int) -> Int =>
{
	return n * n;
};
