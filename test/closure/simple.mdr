import 
{
	"../MidoriPrelude/IO.mdr",
}

def main = fn() : Int
{
	def x : (Int) -> () -> Int = fn(y)
	{
		return fn() : Int { return y; };
	};
	def f = x(5);
	IO::PrintLine(f() as Text); // Should print 5

	return 0;
};