module ClosureInsideConditional

import
{
	"../../MidoriPrelude/IO.mdr",
}

defun foo() : Int =>
{
	defun getFunc() : Int => 1;
	if (true)
	then
	{
		def y : Int = 5;
		getFunc = fn() : Int => y;
	}
	else
	{
		
	};
	return getFunc();
};

IO::PrintLine(foo() as Text);  // Should print 5