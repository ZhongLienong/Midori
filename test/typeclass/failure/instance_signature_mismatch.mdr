module InstanceSignatureMismatch

// Purpose: Reject instance method whose signature does not match the typeclass declaration.

class Show<T> {
    show: fn(value: T) -> Text;
};

// Error: return type should be Text, not Int.
instance Show<Int> {
    def show = fn(value: Int) -> Int => {
        return value;
    };
};
