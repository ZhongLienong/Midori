// Module that defines Show class and Int instance
module ShowTC
public export { Show }

class Show<T> {
    show: fn(value: T) -> Text;
};

instance Show<Int> {
    def show = fn(value: Int) -> Text => {
        return value as Text;
    };
};
