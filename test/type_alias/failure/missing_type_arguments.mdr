module MissingTypeArguments

// Test: a parameterised alias named without any arguments.
// Expected: a diagnostic rather than a template whose parameter nothing binds.

struct Box<T>
{
	item: T
};

alias BoxAlias<T> = Box<T>;

def boxed : BoxAlias = Box(1);
